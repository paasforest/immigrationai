# TypeScript fixes have been pushed to GitHub
# Run these commands on Hetzner to get the fixes:

cd /var/www/immigrationai/backend

# Pull the fixes
git pull origin main

# Build again (should work now)
npm run build

# Restart PM2
pm2 restart immigration-backend

# Check status
pm2 status

# Test API
curl http://localhost:4000/api/intake/services
